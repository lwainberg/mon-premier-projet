---
const year = new Date().getFullYear();

const productLinks = [
  { href: '/#features',   label: 'Features'   },
  { href: '/#technology', label: 'Technology' },
  { href: '/#demo',       label: 'Demo'       },
];

const companyLinks = [
  { href: '/#about',                              label: 'About'   },
  { href: 'mailto:keyur@facetview.net',           label: 'Contact' },
  { href: 'https://www.linkedin.com/company/facetview/', label: 'LinkedIn', external: true },
];
---

<footer class="bg-surface-950 text-surface-400">

  <!-- ── Main footer ──────────────────────────────────────────────────────── -->
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-12">

      <!-- Brand block -->
      <div class="md:col-span-1 flex flex-col gap-5">
        <a href="/" aria-label="Facetview — Home">
          <img
            src="/assets/logo-facetview-white.svg"
            alt="Facetview"
            height="32"
            class="h-8 w-auto"
          />
        </a>
        <p class="text-sm text-surface-500 leading-relaxed max-w-xs">
          The new standard for gem visuals — true-to-life imaging that builds
          buyer confidence and reduces returns.
        </p>
        <!-- Certification -->
        <div class="flex items-center gap-3 mt-1">
          <img
            src="/assets/logo-dpiit-startupindia.png"
            alt="DPIIT #startupindia"
            height="28"
            class="h-7 w-auto opacity-60 grayscale brightness-200"
          />
        </div>
      </div>

      <!-- Links -->
      <div class="md:col-span-2 grid grid-cols-2 gap-8">

        <!-- Product -->
        <div>
          <h3 class="text-xs font-semibold uppercase tracking-widest text-surface-500 mb-5">
            Product
          </h3>
          <ul class="space-y-3 text-sm" role="list">
            {productLinks.map(({ href, label }) => (
              <li>
                <a href={href} class="hover:text-white transition-colors">
                  {label}
                </a>
              </li>
            ))}
          </ul>
        </div>

        <!-- Company -->
        <div>
          <h3 class="text-xs font-semibold uppercase tracking-widest text-surface-500 mb-5">
            Company
          </h3>
          <ul class="space-y-3 text-sm" role="list">
            {companyLinks.map(({ href, label, external }) => (
              <li>
                <a
                  href={href}
                  class="hover:text-white transition-colors inline-flex items-center gap-1"
                  {...(external ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
                >
                  {label}
                  {external && (
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3 opacity-50" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                      <path d="M11 3a1 1 0 100 2h2.586l-6.293 6.293a1 1 0 101.414 1.414L15 6.414V9a1 1 0 102 0V4a1 1 0 00-1-1h-5z"/>
                      <path d="M5 5a2 2 0 00-2 2v8a2 2 0 002 2h8a2 2 0 002-2v-3a1 1 0 10-2 0v3H5V7h3a1 1 0 000-2H5z"/>
                    </svg>
                  )}
                </a>
              </li>
            ))}
          </ul>
        </div>

      </div>
    </div>
  </div>

  <!-- ── Bottom bar ───────────────────────────────────────────────────────── -->
  <div class="border-t border-surface-800">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-5 flex flex-col sm:flex-row justify-between items-center gap-3 text-xs text-surface-600">
      <p>
        © {year} Facetview Imaging Technologies Pvt. Ltd. — Surat, India
      </p>
      <p class="flex items-center gap-2">
        <span>DPIIT Recognised Startup</span>
        <span aria-hidden="true">·</span>
        <span>Reg. DIPP150567</span>
      </p>
    </div>
  </div>

</footer>
